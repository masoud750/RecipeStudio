<Window x:Class="RecipeStudio.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecipeStudio.UI"
        xmlns:vm="clr-namespace:RecipeStudio.UI.ViewModels"
        WindowStartupLocation="CenterScreen"
        xmlns:uc="clr-namespace:RecipeStudio.UI.UserCtrls" 
        d:DataContext="{d:DesignInstance Type=vm:MainVM}"
        mc:Ignorable="d"
        Title="Recipe Studio" Height="450" Width="800"
         Loaded="Window_Loaded"
        >

    <Window.Resources>

        <DataTemplate DataType="{x:Type vm:CategoryTableVM}">
            <uc:CategoryUserCtrl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RecipeTableVM}">
            <uc:RecipeUserCtrl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StepTableVM}">
            <uc:StepUserCtrl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:IngredientTableVM}">
            <uc:IngredientUserCtrl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RecipeQueryTableVM}">
            <uc:QueriesChooserUserCtrl />
        </DataTemplate>

    </Window.Resources>

    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menu -->
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="Auto"/>
            <!-- Caption -->

            <RowDefinition Height="*" MinHeight="150"/>
            <!-- Main content -->
            <RowDefinition Height="Auto"/>
            <!-- Splitter -->
            <RowDefinition Height="*" MinHeight="120"/>
            <!-- Log area -->
        </Grid.RowDefinitions>

        <!-- Menü -->
        <!-- Menü oben -->
        <Menu Grid.Row="0">
            <MenuItem Header="Actions">
                <MenuItem Header="Register" Command="{Binding  RegisterDialogCmd}" />
                <MenuItem Header="Log In" Command="{Binding  LogInCmd}"  />
                <MenuItem Header="Log Out" Command="{Binding LogOutCmd}"  />
            </MenuItem>

        
           

            <MenuItem Header="_Help">
                <MenuItem Header="About ReceipStudio..." />
            </MenuItem>
        </Menu>

        <!-- Toolbar -->
   
        <ToolBarTray Grid.Row="1">
            <ToolBar>
                <Button Content="Recipes"  x:Name="recipesBtn"   Command="{Binding GoToRecipesCmd}" />
                <Button Content="Ingredients" Command="{Binding GoToIngredientsCmd}"/>
                <Button Content="Steps" Command="{Binding GoToStepsCmd}" />
                <Button Content="Categories" Command="{Binding GoToCategoriesCmd}"  />
                <Button Content="Recipe Queries"  Command="{Binding GoToQueriesCmd}"   />
                
            </ToolBar>
        </ToolBarTray>

        <!-- Caption -->
        <DockPanel  Grid.Row="2"   HorizontalAlignment="Stretch" Background="WhiteSmoke" >
            <Label Grid.Row="2" Content="{Binding Session, PresentationTraceSources.TraceLevel=High}" FontSize="8" 
           FontWeight="Bold" DockPanel.Dock="Right" Margin="5" />
            <TextBlock Grid.Row="2" Text="{Binding Caption}" FontSize="16" FontWeight="Bold" Margin="5"/>
            
        </DockPanel>
       

        <!-- Hauptinhalt -->
        <ContentControl Grid.Row="3" Content="{Binding TableController.CurrentVM}" Margin="5"/>

        <!-- Splitter -->
        <GridSplitter Grid.Row="4"
                  Height="5"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center"
                  Background="LightGray"
                  ShowsPreview="True"/>

        <!-- Logbereich -->
        <Grid Grid.Row="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Label -->
                <RowDefinition Height="*"/>
                <!-- TextBox -->
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Log:" FontSize="16" FontWeight="Bold" Margin="5"/>
            <TextBox
                 
                Grid.Row="1"
                 Text="{Binding LogText ,Mode=OneWay}"
                 TextWrapping="Wrap"                 
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 FontFamily="Consolas"
                 FontSize="12"
                 Margin="5"/>
        </Grid>
    </Grid>



</Window>
