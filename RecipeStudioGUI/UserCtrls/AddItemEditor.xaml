<UserControl x:Class="RecipeStudioUI.UserCtrls.AddItemEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecipeStudioUI.UserCtrls"
              xmlns:vm="clr-namespace:RecipeStudioUI.ViewModels"
             mc:Ignorable="d"            
        
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            
        
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
      

        <GroupBox Grid.Row="0" Grid.Column="1" Header="{Binding ColumnHeader}" Margin="5"
                 >
            <ListView ItemsSource="{Binding  AvailableItems}" >
                <ListView.View>
                    <GridView>
                        <!-- Column 1: CheckBox -->
                        <GridViewColumn Header="Select" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate >
                                    <CheckBox 
                                      
                                        IsChecked="{Binding IsSelected, Mode=TwoWay}"  />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Column 2: Category Name -->
                        <GridViewColumn Header="{Binding ColumnHeader}" Width="150"

                DisplayMemberBinding="{Binding Name}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
        <GroupBox Grid.Row="0"   Grid.Column="2"            Header="Selected Items" Margin="5">
            <ListView ItemsSource="{Binding SelectedItems}">
                <ListView.View>
                   
                          <!-- Row 2-->
                    <GridView>
                        <GridViewColumn Header="{Binding ColumnHeader}" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

    </Grid>
</UserControl>
