<Window x:Class="RecipeStudio.UI.Views.EditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecipeStudio.UI.Views" 
        xmlns:viewmodels="clr-namespace:RecipeStudio.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:CategoryVM}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="EditDialog" Height="150" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
           

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="Auto"/>


        </Grid.ColumnDefinitions>

        <!-- Username -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Selected Category:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtUsername" Width="200" Margin="5" 
                 HorizontalAlignment="Left" Text="{Binding UpdateName, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />

        <!--<TextBlock Grid.Row="0" Grid.Column="2"   Text="{Binding LogInfo}" VerticalAlignment="Center" Margin="5" />-->
        <!-- Login Button -->
        <Button Grid.Row="2" Grid.Column="1" Content="Save" Width="100" Margin="5"
            HorizontalAlignment="Right" Click="BtnSave_Click" IsEnabled="{Binding IsValid}"
         />

    </Grid>
</Window>
